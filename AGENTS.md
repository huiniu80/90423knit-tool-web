# 项目规则

## 产品目标

- 这是一个用于绘制、分析毛衣编织图解尺寸及加减针规律的 Web 前端项目。
- 项目的最终目标是：用户提供一个形状后，工具能够将其转换为可用于实际毛衣编织的图解，并给出合理、可执行的尺寸、针格及加减针方案。
- 已实现的功能、交互和数据结构只是当前方案，不应被视为不可改变的前提；即使功能已经完成，也需要根据最终目标判断其合理性。

## 后续改动原则

- 所有新增功能、功能调整、技术设计和交互决策，都必须优先服务于上述最终目标，而不是仅维持现有行为或局部功能。
- 评估方案时，应重点考虑生成结果是否符合实际编织逻辑，尺寸与针数换算是否准确，加减针规律是否合理且可执行，以及图解是否便于用户理解和使用。
- 如果后续需求与最终目标冲突，应明确指出冲突，不得机械照做。
- 如果后续描述不合理、不清楚、存在关键歧义，或不同理解会明显影响产品结果，应先提出具体疑问并向用户确认，再进行实现；不得自行选择可能偏离产品目标的解释。
- 在不影响最终目标且不存在关键歧义时，可以基于现有项目上下文作出合理的小范围假设并继续推进，但应在交付时说明重要假设。

## iPad 与 Apple Pencil 适配规则

- 项目会在 iPad 上通过 Apple Pencil 或触控操作；所有新增功能和功能修改都必须同时考虑 iPad、Apple Pencil 与触控适配，不得只保证鼠标和键盘可用。
- 核心绘制、选择、拖动、缩放、编辑、确认和撤销流程必须能够使用 Apple Pencil 或触控完整操作；不得把悬停、右键或键盘快捷键作为唯一操作入口。
- 交互实现应优先采用 Pointer Events，并正确区分和处理笔、触控与鼠标输入；绘图区域应妥善处理页面滚动、缩放和绘制手势之间的冲突，避免误触、断笔或页面意外移动。
- 控件尺寸、间距、反馈和响应式布局必须适合 iPad 的横屏与竖屏操作；重要操作应提供清晰的触控反馈，并避免依赖过小的点击目标或仅在悬停时显示的关键信息。
- 涉及交互或界面的改动，验收时必须检查 iPad 尺寸下的布局，以及 Apple Pencil/触控等价操作路径；如果当前环境无法进行真实设备验收，应明确说明尚未覆盖的真实设备风险。

## 本地浏览器验收规则

- 需要使用浏览器检查本项目时，必须先启动本地开发服务并通过 HTTP 健康检查确认返回 `200`，再创建浏览器标签页；不得先访问一个尚未启动的本地地址。
- 浏览器验收统一使用 `http://localhost:4173/`。开发服务应以 `npm run dev -- --host localhost --port 4173 --strictPort` 启动，检查与浏览器访问全过程不得在 `localhost`、`127.0.0.1` 或其他端口之间切换。
- 如果首次访问时服务不可用，浏览器会进入受安全策略隔离的错误页。服务恢复后必须关闭该错误标签并新建标签页访问上述地址，不得尝试从错误页跳转或绕过浏览器安全策略。
- 开发服务重启后，应新建浏览器标签页再验收，避免旧标签残留上一进程的 Vite 热更新模块和运行状态。
- 启动预览产生的临时日志必须写入系统临时目录，不得写入仓库；验收结束前应确认浏览器控制台无错误，并确认 Git 工作区未因验收产生额外文件。

## Git 提交规则

- 每次完成代码、文档或配置改动后，都应直接创建本地 Git 提交，并将当前分支推送到远程仓库，无需等待用户再次要求；如果推送失败，应明确说明失败原因。
- 提交信息必须使用简洁、准确的中文，清楚概括本次改动。
- 每次提交只包含本次任务相关的文件，不得混入用户已有或其他任务遗留的未提交改动。

## 产品规格维护规则

- 修改任何功能前，必须先阅读 `docs/说明书总目录.md`、`docs/产品总览.md`、本次涉及的功能说明文档、功能文档直接引用的相关文档，以及 `docs/回归验收清单.md` 中对应的验收项。
- 修改前必须分析并记录：需求涉及哪些功能、会修改哪些数据、哪些计算结果会受到影响、哪些旧功能可能回归，以及是否影响 iPad、Apple Pencil 和触控操作。
- 不得把当前代码行为自动视为正确的产品规则。产品说明书描述的是“产品应该怎样”，不能只是代码实现说明。
- 如果用户需求与说明书中已经确认的规则冲突，必须先向用户说明冲突并等待确认，不得直接改动。
- 如果存在会明显影响产品结果的关键歧义，必须先向用户确认，不得自行选择可能偏离产品目标的解释。
- 如果当前代码与已经确认的产品说明不一致，必须明确指出，不能通过修改说明书来迁就代码。
- 仅根据当前代码、页面或测试整理且尚未经过用户确认的行为，必须保留“根据现状整理”标记。
- 每次修改完成后，必须主动评估本次改动是否影响产品说明书，不得等待用户提醒。评估范围至少包括：功能行为、用户流程、输入输出、数据联动、计算或编织规则、设备交互、异常与边界情况、功能关联、验收标准和待确认问题。
- 如果评估后确认存在影响，必须在同一次任务中同步更新对应的功能说明文档；功能目录、状态或关联关系变化时更新 `docs/说明书总目录.md`；回归范围变化时更新 `docs/回归验收清单.md`；涉及全局规则时更新 `docs/产品总览.md`；用户确认重要产品选择时更新 `docs/产品决策记录.md`。
- 如果评估后确认无需更新说明书，不得为了产生文档改动而机械修改；但必须在交付说明中明确写出“本次无需更新产品说明书”，并简要说明判断依据。
- 每次交付必须使用非技术语言告诉用户：新增了哪些行为、修改了哪些行为、哪些相关行为保持不变、影响了哪些其他功能、产品说明书评估结论及实际更新的文档、新增了哪些待确认问题、实际完成了哪些验证，以及还有哪些未覆盖风险。
