# 项目规则

## 产品目标

- 这是一个用于绘制、分析毛衣编织图解尺寸及加减针规律的 Web 前端项目。
- 项目的最终目标是：用户提供一个形状后，工具能够将其转换为可用于实际毛衣编织的图解，并给出合理、可执行的尺寸、针格及加减针方案。
- 已实现的功能、交互和数据结构只是当前方案，不应被视为不可改变的前提；即使功能已经完成，也需要根据最终目标判断其合理性。

## 后续改动原则

- 所有新增功能、功能调整、技术设计和交互决策，都必须优先服务于上述最终目标，而不是仅维持现有行为或局部功能。
- 评估方案时，应重点考虑生成结果是否符合实际编织逻辑，尺寸与针数换算是否准确，加减针规律是否合理且可执行，以及图解是否便于用户理解和使用。
- 如果后续需求与最终目标冲突，应明确指出冲突，不得机械照做。
- 如果后续描述不合理、不清楚、存在关键歧义，或不同理解会明显影响产品结果，应先提出具体疑问并向用户确认，再进行实现；不得自行选择可能偏离产品目标的解释。
- 在不影响最终目标且不存在关键歧义时，可以基于现有项目上下文作出合理的小范围假设并继续推进，但应在交付时说明重要假设。

## 本地浏览器验收规则

- 需要使用浏览器检查本项目时，必须先启动本地开发服务并通过 HTTP 健康检查确认返回 `200`，再创建浏览器标签页；不得先访问一个尚未启动的本地地址。
- 浏览器验收统一使用 `http://localhost:4173/`。开发服务应以 `npm run dev -- --host localhost --port 4173 --strictPort` 启动，检查与浏览器访问全过程不得在 `localhost`、`127.0.0.1` 或其他端口之间切换。
- 如果首次访问时服务不可用，浏览器会进入受安全策略隔离的错误页。服务恢复后必须关闭该错误标签并新建标签页访问上述地址，不得尝试从错误页跳转或绕过浏览器安全策略。
- 开发服务重启后，应新建浏览器标签页再验收，避免旧标签残留上一进程的 Vite 热更新模块和运行状态。
- 启动预览产生的临时日志必须写入系统临时目录，不得写入仓库；验收结束前应确认浏览器控制台无错误，并确认 Git 工作区未因验收产生额外文件。

## Git 提交规则

- 每次完成代码、文档或配置改动后，都应直接创建本地 Git 提交，无需等待用户再次要求；除非用户明确要求，否则不得推送到远程仓库。
- 提交信息必须使用简洁、准确的中文，清楚概括本次改动。
- 每次提交只包含本次任务相关的文件，不得混入用户已有或其他任务遗留的未提交改动。
